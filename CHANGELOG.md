# MD Music 更新日志

## 1.4.1

### 音源系统回退到 1.2.0 稳定实现

- 实测 1.3.x / 1.4.0 播放仍有问题，音源子系统（解灰服务 + 音源存储 + 导入页 + LX 运行时）整体回退到 1.2.0 的稳定实现。
- 移除上游 1.5.5 的收费配额源架构（聆澜 API 每日 1000 次共享配额）与免费兜底源实验，恢复 1.2.0 的内置音源（QQ 稳定源 / 网易云统一源）+ 用户导入音源 + LX 脚本三层解析。
- 解析顺序：官方地址 → 内置音源 → 用户导入音源 → LX 脚本，与 1.2.0 行为一致。
- 保留 1.5.5 其余改进（播放器交互、登录数据清理、高刷新声明）与本项目的性能优化和播放键重试修复。

## 1.4.0

### LX 脚本音源回归（作为解析链最后一级兜底）

- 移植回 LX 脚本音源功能（上游 1.5.5 曾移除）：设置 → 内置音源预设 → 导入音源，支持粘贴 / 文件导入 LX JavaScript 脚本。
- 解析链变为四级：内置收费源（每日配额）→ 免费兜底源（QQ / 网易云）→ 用户导入音源 → LX 脚本，逐级回退。
- 顺序尝试 LX 脚本并保留请求去重，避免脚本请求风暴。
- 老版本导入过的 LX 脚本数据自动迁移恢复（若未被上游版本清除）。
- 新增 JavaScriptCore 框架依赖（LX 脚本本地运行时）。

## 1.3.1

### 修复 QQ / 酷狗 VIP 歌曲无法播放

- 根因：上游 1.5.5 的 VIP 解析依赖单一共享收费 API（每日仅 1000 次成功配额，全用户共用），配额耗尽当天所有 VIP 歌曲解析全部失败。
- 新增两个免费兜底音源（QQ 稳定源、网易云统一源，无配额限制）：收费源可用时优先使用，配额耗尽自动切换兜底源，保证 QQ / 网易云 VIP 歌曲全天可播。
- 修复加载失败后点播放键会播放上一首歌的问题：现在会自动重试加载当前歌曲。

### 已知限制

- 酷狗 VIP 暂无免费兜底源：每日配额耗尽后酷狗 VIP 歌曲仍可能间歇性无法播放（次日恢复）。

## 1.3.0

### 同步上游 1.5.5

- 同步上游 1.5.5 全部改进：网页登录数据清理、播放器交互与稳定性修复。
- 播放器顶栏标题区可点击打开更多操作菜单。
- 支持 iPhone 高刷新率声明（CADisableMinimumFrameDurationOnPhone）。
- 上游移除了 LX 脚本音源功能，本版本跟随移除（如需恢复请反馈）。
- 随机播放、锁屏封面与播放进度刷新沿用本项目优化实现，并与上游方案融合（0.2s 回调 + 节流、封面缓存去重走 CoverLoader）。

### 保留的既有优化

- 封面共享加载器（CoverLoader）+ 缩略图 + ImageIO 降采样。
- 发现页接口独立降级、静默刷新。
- 本地歌单/收藏防抖后台写入 + 退后台落盘。
- 壁纸备份仅变化时回写。

## 1.2.0

### 修复主页刷不出来

- 发现页各接口改为独立降级：此前任一接口失败会丢弃整页数据（"主页刷不出来"的主要根因），现在单个板块失败只影响该板块，全部失败才提示重试。
- 首页已有内容时刷新改为静默刷新，不再把整页闪成加载转圈。
- 免责声明确认前不再预加载网络内容。

### 启动与首页性能（针对"进入 App 卡顿"）

- 首页封面全面接入缩略图（榜单 120px、每日推荐与歌单广场 300px），首帧不再并发解码 30+ 张全尺寸大图。
- 封面解码引入 ImageIO 降采样，超长边 1024px 封顶，个别超大图不再拖慢首帧。
- 壁纸 base64 备份仅在内容变化时回写，去掉每次启动一次的大字符串同步写。

### 回归修复（1.1.0 引入）

- 收藏防抖改为网易云 / QQ 各自独立，修复连续收藏两个平台时其中一个写入被取消的问题。
- 退后台时立即落盘防抖数据，修复上滑杀掉 App 可能丢失最近改动的问题。
- 修复随机模式删除"正在播放的歌"后播放顺序指针未对齐、下一首可能重播当前歌的问题。
- 修复快速切歌时锁屏封面可能被旧请求覆盖串图的问题。
- 封面命中缓存时不再闪一帧占位图。

## 1.1.0

### 性能优化（参照 SwiftUI 社区通用方案：NSCache 内存缓存 + .task(id:) 自动取消 + 缩略图降采样）

- 新增全局共享封面加载器：列表封面、锁屏歌词卡、播放页模糊背景、主色提取共用同一份下载与解码结果，同一封面不再被 4 处重复请求；封面磁盘缓存 120MB，冷启动后滚动列表也能秒出图。
- 歌曲列表 46pt 小封面改用网易云 120×120 缩略图，不再加载 300~640px 原图，滚动更流畅、更省流量。
- 播放进度回调从 0.1s 放宽到 0.25s：迷你播放器与歌词区不再每秒全量重绘 10 次，降低持续 CPU 占用与发热。
- 锁屏封面同曲去重：此前每次拖进度条/暂停/恢复都会重新下载一次封面。
- 本地歌单与收藏改为防抖 + 后台编码写入：上千首的歌单每次增删不再阻塞主线程。
- 播放页模糊背景缓存加上限（12 张 / 24MB），长时间听歌不再无限累积内存。

### 修复

- 修复随机模式下删除队列歌曲时，把"歌曲 id"误当"队列下标"过滤导致的播放顺序错乱问题。

## 1.0.0

### MD Music 首个版本

- 基于 Beans Music 1.5.4 定制：聚合网易云音乐 / QQ 音乐 / 酷狗，纯 SwiftUI。
- 自定义应用图标与欢迎页 Logo。
- 同步上游 LX 脚本音源支持（JavaScriptCore 运行时）。
- 包含长歌名布局修复、歌手主页分页加载、酷狗加载风暴修复等上游 1.5.4 全部改进。
- 修复上游 ProfileView / SettingsView 缺失定义与 UnblockSourceStore 初始化顺序两处编译错误。
- 更新检测指向本仓库（JIEGEjjj/MD-Music），App 内更新直接下载自建版本。

## 1.5.4

### 新增与优化

- 同步上游 1.5.4 全部内容：修复长歌名撑宽歌曲列表布局，统一使用有限宽度和尾部截断。
- 修复歌手主页只加载 30 首歌曲的问题，网易云音乐和 QQ 音乐改为分页加载更多歌曲。
- 重点修复酷狗主页加载任务重复触发，导致结果无限返回和网络请求风暴的问题。
- 调整播放器底部循环按钮与播放列表按钮为左右对称默认位置。
- 歌手主页背景同步主页壁纸。
- LX 脚本音源支持（JavaScriptCore 运行时）。

### 修复

- 修复上游 ProfileView/SettingsView 缺失 sourceStore、importedSourceCount 定义导致无法编译的问题。
- 修复上游 UnblockSourceStore 初始化顺序错误导致无法编译的问题。

## 1.5.3

### 新增与优化

- 完善配置备份与恢复，支持保存壁纸、字体、播放器布局、自定义音源和本地歌单等设置。
- 我的页面新增交流群入口，可直接查看群二维码。
- 歌词页面支持自定义背景图片、背景模糊，并可同步到封面播放页。
- 设置新增平台显示管理，主页、搜索、音乐库和登录入口按选择显示平台。
- 主页歌单广场支持收起和展开，播放设置支持收缩分组。

### 修复

- 修复壁纸备份只保存旧路径，恢复后图片不显示的问题。
- 修复排行榜详情说明区域出现白块、玻璃效果不一致的问题。
- 修复隐藏平台后部分页面仍显示该平台的问题。
- 修复自定义歌词背景导致播放器界面位置偏移的问题。
- 优化评论区、榜单详情页与播放器设置的背景显示和页面流畅度。

## 1.5.2

### 新增与优化

- 备份功能覆盖更多已调试设置，包含壁纸、字体、播放器布局、自定义音源、本地歌单等本机配置。
- 我的页面底部新增“交流群”入口，点击后可直接查看群二维码。
- 榜单详情页顶部信息卡恢复与应用整体一致的液态玻璃效果。
- 首次引导页新增平台选择，可按需要只显示网易云、QQ 音乐或酷狗音乐中的部分平台。
- 设置页新增“平台显示”，可随时重新选择需要展示的平台。
- 主页歌单广场新增收缩/展开，默认收起减少首页长度。
- 播放器歌词界面支持上传自定义背景图，并可调节背景模糊强度。

### 修复

- 修复打开任意排行榜详情后，榜单说明区域出现白块背景的问题。
- 修复备份导出范围不完整的问题，同时排除账号登录信息、搜索记录和日志。
- 修复壁纸备份只保存旧沙盒路径、没有补齐图片内容，导致恢复后壁纸丢失的问题。
- 修复隐藏某个平台后，主页、搜索、音乐库、账号登录等入口仍显示该平台的问题。
- 修复恢复备份时可能写回隐私记录或运行态标记的问题。

## 1.5.1

### 新增与优化

- 播放进度刷新拆分为独立时钟，播放中浏览主页、搜索、音乐库、我的页面时不再触发整页高频重绘。
- 播放器设置改为全屏打开，打开后暂停播放器页面 UI 渲染但保持歌曲继续播放。
- 播放器设置支持收缩分组、紧凑布局和强制液态玻璃卡片，提升播放中调整设置的响应速度。
- 新增全局主文字颜色自定义，和注释文字颜色一样作用于搜索、歌单、我的、设置等页面。
- 每日推荐、排行榜、QQ 歌单详情新增搜索和随机播放。
- 壁纸背景同步到播放器设置、账号登录、排序设置、歌单、排行榜和每日推荐详情。

### 修复

- 修复播放中滑动主页排行榜、打开播放器设置或应用设置时明显卡顿发热的问题。
- 修复播放器右上角更多菜单和播放器设置内部分控件偶发需要点多次的问题。
- 修复排行榜“热歌榜”等板块在自定义壁纸下出现大块白底的问题。
- 恢复主页排行榜板块的液态玻璃卡片质感，不再显示为过度透明的纯色层。
- 修复备份会包含账号 cookie、token 和用户资料的问题；恢复旧备份时也会跳过账号信息。
- 删除设置页导入日志入口，并将导出日志移入查看日志菜单。

### 使用说明

- 本版本的备份文件不会包含账号信息，换机或恢复备份后需要重新登录各平台账号。
- 播放器设置全屏打开时歌曲会继续播放，只暂停播放器页面 UI 渲染以降低卡顿和发热。
- 支持 iOS 15 及以上系统。
- 未签名 IPA 请使用 Sideloadly、AltStore 或爱思助手自行签名安装。

## 1.5.0

### 新增与优化

- 优化酷狗音乐相关功能，改善搜索、歌单同步与播放链路。
- 内置音源无需登录账号即可尝试播放大部分会员歌曲。
- 支持自定义导入音源，作为官方播放地址不可用时的备用来源。
- 简化「我的」界面布局，减少不必要的入口和层级。
- 新增 GitHub 自动检测更新功能，并在发现新版本时显示更新内容。
- 更新弹窗支持直接下载最新版 IPA，下载完成后自动呼出系统分享面板。

### 修复

- 优化第三方音源匹配与播放地址解析。
- 优化设置页面滚动、页面切换及相关交互的流畅度。
- 修复部分更新提醒和版本说明显示问题。

### 使用说明

- 酷狗音乐第三方音源会受到会员状态、版权和地区限制。
- 导入音源仅作为备用来源，不保证所有歌曲都可以播放。
- 支持 iOS 15 及以上系统。
- 未签名 IPA 请使用 Sideloadly、AltStore 或爱思助手自行签名安装。

### 免责声明

本应用为第三方开源音乐客户端，仅供个人学习交流使用，并非网易云音乐、QQ 音乐或酷狗音乐官方软件。音乐版权归相应版权方所有，请遵守各平台服务条款并支持正版。
